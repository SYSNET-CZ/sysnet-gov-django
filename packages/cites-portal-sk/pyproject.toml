[project]
name = "sysnet-cites-portal-sk"
version = "0.1.0"
dependencies = [
    "fastapi",
    "jinja2",
    "sysnet-cites-portal-core",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
